# Runs the SpectraForge backup nightly at 03:00 (and catches up if the box was
# off at the scheduled time).
[Unit]
Description=Nightly SpectraForge backup

[Timer]
OnCalendar=*-*-* 03:00:00
Persistent=true

[Install]
WantedBy=timers.target
